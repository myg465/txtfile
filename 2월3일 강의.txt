(중요)클래스의 첫문자는 대문자, 변수와 메소드는 소문자로 시작한다.
숫자를 문자열로 바꾸기 숫자+""="숫자"
Integer.parseInt(문자열) -> 문자열을 숫자로 바꾸기

실수 기본형 double

printf  -여러개의 변수를 동시출력할수 있음, 소수점 자리수 지정가능, 자동 줄바꿈은 안됨(%n으로 구분)
메소드-함수

논리연산자- 피연산자가boolean, 연산결과도 boolean
and && /  or ||

삼항연산자 연산결과가 참이면 식1로  거짓이면 식2의 결과를 반환(연산자 안에 연산자가 들어갈수도 있음)
(조건식)? 식1:식2

ex) x>=0?x:-x;                ->음수를 양수로 바꾸는 식(절대값)
                                       Math.abs함수 써도됨

대입연산자

조건문
if, switch

조건식과 실행될 하나의 문장 또는 블럭으로 구성됨.
swiitch는if 로 바꿀수 있지만, if는 switch로 변경안됨(속도는 switch가 빠르다.)
switch는 case로 구분됨(비교를 안해도됨)-경우의 수가 많은경우 switch가 유리
case뒤에는 꼭 break를 넣어야됨(break를 안씀으로서 활용하는 방법도 있다.
(게시판의 등급에따른 권한부여 등))
switch는 숫자와 문자만 가능
DB에서 자료를 받아서 비교할수도 있다.


간단한 조건은 삼항식이 좋다.

반복문
for,while,do-while

변수는 데이터값을 저장하고, 객체는 데이터주소를 저장한다.

if의 형태
if단독, if-else if(아닐경우 중첩),if-(if-else)-else(맞을경우 중첩)

비교할변수.equalsIgnoreCase(비교할변수)->string비교시 대소문자 구별없이 비교할수있음

!(비교) ->전체가 아니다.

and가 or보다 우선순위 높음
str!=null&&!str.equals("") -> 아무것도 안적혀있을때를 검사하는 구문

변수선언을 하면 다음번에 변수이름만 쓰면됨



