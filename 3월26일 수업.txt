태그의 스타일을 변경하기
ex)  
document.getElementById("s01").style.width="600px";
-> 이미지 확대하기
j.quary로 더짧게 변경가능
폼에서 값을 넘겨서 받는곳이 jsp/servret임

jsp-> html안에 자바코드를 넣을수 있다.

---------------------------------제이쿼리-----------------------------------

ppt참조

많이 사용하는 자바스크립트 구문들을 더 쉽게 사용하게 만든것(더 짧게)
마우스 이벤트 처리,css변경,애니메이션
css에서 적용되는 문법을 적용할수 있다.

*html은 위에서 아래로 순차적으로 실행됨


제이쿼리를 시작하는 구문
js파일에 $(document).ready(function(){
     (이안에 구문을 입력)	
});  ->화면이 다읽혀진 다음(html구문이)에 실행하라는 뜻

선택자-> $("선택자").메소드
선택자는아이디 뿐만 아니라 클래스,태그도 될수 있다.

.hide(사라지게함)
.show(나타나게함)
선택자 this (자기자신을 가리킴 (따옴표 안써도됨))

404에러 - 파일을 찾을수 없다는 얘기임
(경로를 찾을수 없거나,파일이 없을경우)

클릭은 어떠한 태그든지 적용가능

:nthchild(n) ->eq(n) ->몇번째인지 표시

:first-> 첫번째

[href]->속성선택

p span->자손선택

다른 요소를 선택해서 다른요소의 스타일을 변경할수 있다.

아이디와 클래스를 동시에 쓸수있다.
태그이름은 임의로 정해서 할수 있다.

마우스 엔터(hover과 똑같음) - 마우스 리브(마우스가 떠날때) 한쌍으로 써야됨

마우스 다운(버튼을 누르고 있을때) 마우스 업(버튼을 땠을때)

클래스를 만들어 두고 액션이 들어왔을때 클래스를 넣는다

text() -> 텍스트를 넣어준다.(대체함)

focus-> input에 커서가 간 순간 실행

제어문,반복문, 연산자 등 사용가능

ul(공지사항(바탕색 흰색,글자 #efefef / 마우스 오버시 바탕색 검정 글자 흰색
p를 밑에 만들어서 공지사항이 선택되었습니다. 빠져나가면 원래대로)
,이벤트,로그인,회원가입 )

