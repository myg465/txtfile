form태그 계속

웹에서 입력되는 모든데이터는 String
자바스크립트의 데이터타입은 var
name=>변수명
value=>변수값
text형태의 input 은 입력되는 값이 변수값
id=> css를 적용할때 쓰는 주소값

콘솔에서 구현되는 내용들이 입력값을 웹에서 받는 걸로 바뀜

체크박스는 name이 다 달라야함(라디오 버튼은 똑같아도 됨)-중복선택가능하기 때문에

체크박스-배열로 받음

button-css에서 받을수 있음, 자바스크립트를 거치도록 설정가능
기본값

span은 width를 맞출수 없음
display: inline-block-> 인라인을 블록으로 바꿈

---------------------------------html끝--------------------------------------

--------------------------------css시작--------------------------------------
주석다는 방법
/*   */

외부 스타일 시트
<link href="css/style.css" type="text/css" rel="stylesheet">
이렇게 선언
head부분에 한다.
전체선택자-> *{} 
전체적으로 적용해야 되는 부분

클래스 선택자 -재사용가능

label-> 인라인 방식

두개이상 태그를 , 로 묶어서 공통부분을 정의해 줄수도 있음

케스케이팅- 마지막에 적용된 부분이 적용된다.(순서중요)

클래스보다 아이디가 우선적용(같은 위치에 동시 적용될 경우)

제일 우선적용되는것은 인라인 스타일

스타일 상속
상속관계를 구별하기 위해 들여쓰기를 함

css가 브라우저에 따라 적용이 안될것을 대비하여
접두어를 붙임(접두어를 자동으로 붙여주는 js가 있음)

font-family->폰트를 정해주는 속성
웹폰트-> 구글 웹폰트, em 상대적 크기

video-muted해야 autoplay됨 (웹표준)

박스안의 글자 위아래 정렬하기
line-height

text-align: justify; ->글자를 양쪽으로 정렬

text-overflow->영역을 벗어나는 텍스트
ellipsis-... 으로 나옴
clip-그냥자름

박스모델
span- 그냥쓰면 티가 안남

display옵션을 사용하여 사라지게 할수있다(반응형 웹)

boxsizing속성 기준점을 정한다.(박스의)

float - 블럭속성의 박스들을 옆으로 붙게 할수 있다. 

