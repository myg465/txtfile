String[]args->args라는 문자열 배열
기본값

이름의 뒤에 괄호가 있으면 메소드(함수)임 

char 'a'를 쓰면 실제 들어가는 값은 97이 들어감
대문자와 소문자의 차이는 32임

input.length();//문자열의 길이를 측정
input.toUpperCase();//소문자 문자열을 모두 대문자로 바꾸는 명령
input.toLowerCase();//대문자 문자열을 모두 소문자로 바꾸는 명령
순위
1등을 비교해서 
프로그램의 구성 쓰기, 수정,삭제

숙제 성적처리 프로그램 개발해서 게시판에 올리기
